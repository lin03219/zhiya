# 项目概述
- 项目名称：Bybit 质押借币桌面工具
- 技术栈：Python + tkinter + pystray + PyInstaller + Cython
- 当前版本：v4.1.1
- 仓库：https://github.com/lin03219/zhiya

# 开发规范
- 中文注释
- 文件名 snake_case，类名 PascalCase，函数名 camelCase
- 不引入不必要的第三方依赖
- ❌ 用户说"改"才写代码，否则只出计划
- ❌ 不要做与当前任务无关的改动
- ❌ 不要自动 git commit

# 模块状态
| 模块 | 职责 | 状态 |
|------|------|------|
| 配置管理 | API 密钥、代理、网络切换、LTV阈值、借币速率、保护参数、LTV纠错参数 | ✅ |
| API 通信层 | Bybit V5 签名、HTTP 封装、三档限流检测、VPN 延迟测试 | ✅ |
| 质押借币业务 | 余额查询、质押借币、还款、持仓、利息、划转、最大可借、调整抵押品 | ✅ |
| 闪兑 | 小币种一键兑换 USDT、限额过滤、报价过期重试、批量兑换 | ✅ |
| 保护功能 | LTV 超限自动追加抵押（划转 USDT 到统一账户） | ✅ |
| IP 限制 | Worker 端 IP 白名单校验（Cloudflare） | ✅ |
| 通知推送 | 飞书/钉钉：借币成功、LTV 超限警告、LTV 阈值提醒、配额不足、三档限流 | ✅ |
| 桌面界面 | 余额概览、账内划转、借币操作、借贷记录、持仓管理、设置、调整抵押品 | ✅ |
| 系统托盘 | 最小化到右下角，左键恢复，右键退出 | ✅ |
| 在线更新 | 版本检测 → 下载 → 杀进程 → 替换 → 重启 | ✅ |
| 更新日志 | 版本号可点击查看 CHANGELOG | ✅ |
| 代码加固 | Cython 编译 .pyd + PyArmor + XOR URL 加密 | ✅ |

# 功能详情

## v4.0.0
- 修复卡顿问题
- 9 个核心模块 Cython 编译为 .pyd 二进制
- GitHub/Worker URL XOR 加密防提取

## 调整抵押品
- 持仓窗口内每行"调整"按钮 → 追加/减少抵押品弹窗
- LTV 实时同步，零额外 API 消耗
- 余额截断防虚标

## 借币设置合并
- LTV 纠错 + 保护 + 飞书提醒合并为统一设置面板
- 新增目标 LTV 参数（上限 78%）
- 抵押品公式修复，日志上限 20 条

## 多币并行借币
- "+" 按钮动态添加借币行，最多 5 个币种同时借
- 每行独立：币种输入、数量输入、计算按钮、发起/停止借币
- 每行独立借币循环，互不影响
- 每行独立"已借到"按钮和飞书通知

## 还款
- 智能 5 步还款：查资金余额 → 用现币还 → 轮询 position → 剩余换算 USDT → 抵押品补清
- 手动还币弹窗、一键还清单币、一键还清全部

## 持仓
- 借入币种、欠款、利率、统一持有、资金持有
- 每行：划转按钮 + 调整抵押品按钮 + 还款按钮

## 账内划转
- 默认 USDT，支持"全部"按钮
- 资金 ↔ 统一账户，异步不卡 UI
- 确认弹窗居中于主窗口

## 闪兑
- 小币种 → USDT，限额过滤，一键批量兑换
- 报价过期自动重试（最多 3 次）

## LTV 自动纠错
- 借币循环中 LTV 超限 → 自动停止 → 等待 → 按借币目标 LTV 重算 → 静默自动重新发起
- 参数：总开关 / 连续失败次数 / 等待时间 / 自动发起（已去掉冗余比例）

## 三档限流处理
- ①档：连续3次接口限流(10006) → 停止全部借币 + 橙色弹窗(一键去调整速率) + 飞书提醒
- ②档：HTTP 429 → 停止全部借币 + 红色弹窗实时倒计时 + 借币按钮禁用6分钟 + 飞书提醒
- ③档：HTTP 403 → 停止全部借币 + 深红弹窗 + 借币按钮禁用30分钟 + 飞书提醒
- 限流事件写入借贷记录

## 自动保护
- LTV 超阈值 → 自动划转 USDT 追加抵押

## 界面优化
- 统一账户分两行显示（可用/其他不被截断）
- 余额 2 位小数统一
- API 限额 1 秒实时刷新
- LTV 统一 2 秒刷新（借币中/非借币）
- 持仓窗口 700×450
- 划转/借币确认弹窗居中
- ToolTip 悬停提示

## 通知推送
- 借币成功飞书/钉钉通知
- LTV 超限警告 + 阈值提醒
- 平台配额不足连续失败飞书提醒（可开关）

## 设置
- API 密钥 / 网络 / 代理 / 飞书钉钉 Webhook
- 借币请求速率（1.0~4.0 秒，随机加 0.01~0.15 秒）
- LTV 飞书提醒阈值 + 推送间隔
- LTV 自动纠错参数
- 借币设置合并面板

## 在线更新
- 自动检测 + 手动检查更新
- 动态文件名 + 双杀进程

## 系统托盘
- 最小化到右下角，左键恢复，右键退出
