# 项目概述
- 项目名称：Bybit 质押借币桌面工具
- 技术栈：Python + tkinter + pystray + PyInstaller
- 当前版本：v3.5
- 仓库：https://github.com/lin03219/zhiya

# 开发规范
- 中文注释
- 文件名 snake_case，类名 PascalCase，函数名 camelCase
- 不引入不必要的第三方依赖
- ❌ 用户说"改"才写代码，否则只出计划
- ❌ 不要做与当前任务无关的改动
- ❌ 不要自动 git commit

# 模块状态
| 模块 | 职责 | 状态 |
|------|------|------|
| 配置管理 | API 密钥、代理、网络切换、LTV阈值、借币速率、保护参数、LTV纠错参数 | ✅ |
| API 通信层 | Bybit V5 签名、HTTP 封装、限流检测、VPN 延迟测试 | ✅ |
| 质押借币业务 | 余额查询、质押借币、还款、持仓、利息、划转、最大可借 | ✅ |
| 闪兑 | 小币种一键兑换 USDT、限额过滤、报价过期重试、批量兑换 | ✅ |
| 保护功能 | LTV 超限自动追加抵押（划转 USDT 到统一账户） | ✅ |
| IP 限制 | 启动时校验 IP 白名单 | ✅ |
| 通知推送 | 飞书/钉钉：借币成功、LTV 超限警告、LTV 阈值提醒 | ✅ |
| 桌面界面 | 余额概览、账内划转、借币操作、借贷记录、持仓管理、设置 | ✅ |
| 系统托盘 | 最小化到右下角，左键恢复，右键退出 | ✅ |
| 在线更新 | 版本检测 → 下载 → 杀进程 → 替换 → 重启 | ✅ |

# 功能详情

## 借币
- 输入币种 → 自动计算最大可借（资金账户 USDT × 80% × 85% 冗余）
- 循环借币：可配速率（1.0~4.0 秒）+ 随机 0.01~0.5 秒浮动防封
- 借币成功 → 红色"已借到"按钮 + 飞书循环通知直到点击
- API 限额检测（X-Bapi-Limit-Status），被封自动暂停
- 借贷记录：时间/方向/币种/数量/原因，实时显示

## LTV 自动纠错（v3.4 新）
- 借币循环中 LTV 超限次数达标 → 自动停止 → 等待 N 秒 → 重算最大可借 → 按冗余比例填入 → 自动重新发起
- 齿轮按钮设置 5 参数：总开关 / 连续失败次数 / 等待秒数 / 冗余比例 / 自动发起

## 持仓
- 显示借入币种、欠款、利率、统一持有、资金持有
- 每行：划转按钮（全部填入）+ 还款按钮（单个/一键还清含利息）

## 账内划转
- 默认 USDT，支持"全部"按钮自动填余额
- 资金 ↔ 统一账户互转

## 闪兑
- 小币种 → USDT，限额过滤，一键批量兑换
- 报价过期自动重试（最多 3 次），逐币重新询价

## 自动保护
- LTV 超阈值 → 自动划转 USDT 追加抵押
- 可配：触发 LTV / 单笔金额 / 统一账户最低余额

## 设置
- API 密钥 / 网络切换 / 代理 / 飞书钉钉 Webhook
- 借币请求速率（1.0~4.0 秒）
- LTV 飞书提醒阈值 + 推送间隔（1~60 秒）
- LTV 自动纠错参数

## 在线更新
- 启动后自动检测 + 每 30 分钟重试 + 手动"检查更新"按钮
- 有新版本显示按钮 → 点击下载 → 自动替换旧 EXE → 重启
- 动态文件名 + 双杀进程 + move 错误重试

## 系统托盘
- 最小化/关闭 → 隐藏到右下角图标
- 左键恢复 / 右键退出
- 去重（保存设置不会重复创建）