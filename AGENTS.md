# 项目概述
- 项目名称：Bybit 质押借币桌面工具
- 技术栈：Python + tkinter + pystray + PyInstaller
- 当前版本：v3.8
- 仓库：https://github.com/lin03219/zhiya

# 开发规范
- 中文注释
- 文件名 snake_case，类名 PascalCase，函数名 camelCase
- 不引入不必要的第三方依赖
- ❌ 用户说"改"才写代码，否则只出计划
- ❌ 不要做与当前任务无关的改动
- ❌ 不要自动 git commit

# 模块状态
| 模块 | 职责 | 状态 |
|------|------|------|
| 配置管理 | API 密钥、代理、网络切换、LTV阈值、借币速率、保护参数、LTV纠错参数 | ✅ |
| API 通信层 | Bybit V5 签名、HTTP 封装、限流检测、VPN 延迟测试 | ✅ |
| 质押借币业务 | 余额查询、质押借币、还款、持仓、利息、划转、最大可借 | ✅ |
| 闪兑 | 小币种一键兑换 USDT、限额过滤、报价过期重试、批量兑换 | ✅ |
| 保护功能 | LTV 超限自动追加抵押（划转 USDT 到统一账户） | ✅ |
| IP 限制 | Worker 端 IP 白名单校验（Cloudflare） | ✅ |
| 通知推送 | 飞书/钉钉：借币成功、LTV 超限警告、LTV 阈值提醒 | ✅ |
| 桌面界面 | 余额概览、账内划转、借币操作、借贷记录、持仓管理、设置 | ✅ |
| 系统托盘 | 最小化到右下角，左键恢复，右键退出 | ✅ |
| 在线更新 | 版本检测 → 下载 → 杀进程 → 替换 → 重启 | ✅ |

# 功能详情

## 多币并行借币（v3.8 新）
- "+" 按钮动态添加借币行，最多 5 个币种同时借
- 每行独立：币种输入、数量输入、计算按钮、发起/停止借币
- 每行独立借币循环，互不影响
- 每行独立"已借到"按钮和飞书通知
- 行删除按钮（借币中禁用）

## 还款（v3.7）
- 智能 5 步还款：查资金余额 → 用现币还 → 轮询 position → 剩余换算 USDT → 抵押品补清
- 手动还币弹窗、一键还清单币、一键还清全部

## 持仓
- 借入币种、欠款、利率、统一持有、资金持有
- 每行：划转按钮 + 还款按钮

## 账内划转
- 默认 USDT，支持"全部"按钮
- 资金 ↔ 统一账户，异步不卡 UI

## 闪兑
- 小币种 → USDT，限额过滤，一键批量兑换
- 报价过期自动重试（最多 3 次）

## LTV 自动纠错
- 借币循环中 LTV 超限 → 自动停止 → 等待 → 重算 → 自动重新发起
- 齿轮按钮 5 参数：总开关 / 次数 / 等待 / 冗余比例 / 自动发起

## 自动保护
- LTV 超阈值 → 自动划转 USDT 追加抵押

## 设置
- API 密钥 / 网络 / 代理 / 飞书钉钉 Webhook
- 借币请求速率（1.0~4.0 秒）
- LTV 飞书提醒阈值 + 推送间隔
- LTV 自动纠错参数

## 在线更新
- 自动检测 + 手动检查更新
- 动态文件名 + 双杀进程

## 系统托盘
- 最小化到右下角，左键恢复，右键退出